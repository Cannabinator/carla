# Mosquitto MQTT Broker Configuration
# Used by CARLA V2V Research Platform

# Listen on default MQTT port
listener 1883

# Allow anonymous connections (suitable for local research)
allow_anonymous true

# Logging
log_type all
log_dest stdout
